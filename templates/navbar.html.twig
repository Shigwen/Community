
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

	<a class="navbar-brand bg-primary rounded-pill pl-3 pr-3" href="#">Community Project</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
		aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarNav">
		<ul class="navbar-nav">

			<li class="nav-item active border border-primary">
				<a class="nav-link" href="{{ path('home') }}">Home <span class="sr-only">(current)</span></a>
			</li>
			<li class="nav-item border border-primary">
				<a class="nav-link" href="{{ path('user_account') }}">Profile</a>
			</li>
			<li class="nav-item border border-primary">
				<a class="nav-link" href="{{ path('events') }}">Calendar</a>
			</li>

			{% if is_granted('ROLE_RAID_LEADER') %}

				<li class="nav-item border border-primary">
					<a class="nav-link" href="{{ path('raidleader_events') }}">RL HQ</a>
				</li>

				<li class="nav-item border border-primary">
					<a class="nav-link" href="{{ path('raidleader_manage_participant_ban_players') }}">Ban Hammer</a>
				</li>

			{% endif %}

			<li class="nav-item border border-primary">
				<a class="nav-link" href="#">About us</a>
			</li>

		</ul>
	</div>
</nav>
