
<div class="row d-flex justify-content-around">

    <div class="col-12 col-md-11 p-md-4 p-xs-1 border border-primary rounded col-margins">

        <h2 class="text-center h3">Character list</h2>

        <table class="table table-striped table-bordered table-hover table-dark">
            
            <thead class="text-center">
                <tr>
                    <th scope="col">Faction</th>
                    <th scope="col">Name</th>
                    <th scope="col">Server</th>
                    <th scope="col">Class</th>
                    <th scope="col" class="d-none d-md-table-cell">Role</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>

            <tbody class="text-center">
                {% for character in characters %}
                    <tr>
                        <td>{{ character.faction.name }}</td>
                        <td>{{ character.name }}</td>
                        <td>{{ character.server.name }}</td>
                        <td>{{ character.characterClass.name }}</td>
                        <td>{{ character.strRoles }}</td>
                        <td>
                            <button type="button" class="btn btn-primary rounded-pill" data-toggle="modal" data-target="#clickMeCharacter_{{ character.id }}">Show notes</button>
                            <a href="{{ path('user_account')}}?id={{ character.id }}" class="btn btn-primary rounded-pill">Modify</a>
                            <a href="{{ path('user_archive_character', {id: character.id})}}" class="btn btn-primary rounded-pill">Delete</a>
                            {{ include('user/modal/_character_show_notes.html.twig') }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>

        </table>
    </div>

</div>